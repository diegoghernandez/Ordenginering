---
import Customize from '@/components/pages/Customize.astro'
import pizzaList from '@/data/pizza.json'
import { es } from '@/i18n/pages/Customize.json'
import type { GetStaticPaths, InferGetStaticParamsType } from 'astro'

export const getStaticPaths = (() => {
	return pizzaList.es
		.map((element) => ({ params: { pizza: element.name.replace(' ', '-').toLowerCase() }}))
		.concat({ params: { pizza: 'vacia' }})
}) satisfies GetStaticPaths

type Params = InferGetStaticParamsType<typeof getStaticPaths>

const { pizza } = Astro.params as Params
---

<Customize pizza={pizza} t={es} />