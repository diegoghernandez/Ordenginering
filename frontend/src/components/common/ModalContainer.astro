<dialog>
   <nav>
      <button aria-label='Close menu'>
         <svg xmlns="http://www.w3.org/2000/svg" fill="none" stroke="currentColor" viewBox="0 0 24 24" class="humbleicons hi-times">
            <g xmlns="http://www.w3.org/2000/svg" stroke="currentColor" stroke-linecap="round" stroke-width="2"><path d="M6 18L18 6M18 18L6 6"/></g>
         </svg>
      </button>
      <slot name="description" />
   </nav>
   <slot />
</dialog>

<style>
   dialog {
      width: 100%;
      max-width: 640px;
      min-height: 100vh;
      border: none;
      background-color: var(--background);
      & nav {
         display: flex;
         width: 100%;
         justify-content: end;
         gap: 16px;
         position: sticky;
         top: 0;
         z-index: 99999;
      }
      & nav:has(p) {
         justify-content: start;
      }
      &::backdrop {
         opacity: 0.1;
         background-color: var(--state-layer-color);
      }
   }

   dialog:has(p) {
      margin-left: auto;
   }

   button {
      background-color: transparent;
      border: none;
   }
</style>