name: Publish pizza register service image

on:
   push:
      paths: [backend/pizza-register-service/**, .github/workflows/pizza_register_service_workflow.yaml, '!**.md', '!**.MD']
      branches:
         - main 

concurrency:
   group: ${{ github.workflow }}-${{ github.ref }}
   cancel-in-progress: true

jobs:
   test:
      runs-on: ubuntu-22.04
      defaults:
         run:
            working-directory: backend/pizza-register-service
      steps:
         - uses: actions/checkout@v4
           with:
            sparse-checkout: backend/pizza-register-service
            fetch-depth: 0
         - uses: actions/setup-java@v4
           with:
            distribution: 'temurin'
            java-version: '21'
            cache: 'maven'
         - name: Test and publish image
           run: ./mvnw spring-boot:build-image -Ddocker-password=${{ secrets.DOCKER_PASSWORD }}