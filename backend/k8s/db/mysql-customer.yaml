apiVersion: v1
kind: Service
metadata:
  name: mysql-customer-svc
spec:
  selector:
    app: mysql-customer
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
  type: ClusterIP

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-customer
  labels:
    app: mysql-customer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-customer
  template:
    metadata:
      name: mysql-customer
      labels:
        app: mysql-customer
    spec:
      containers:
        - name: mysql-customer
          image: mysql:8.2.0
          imagePullPolicy: IfNotPresent
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: -ppassword
            - name: MYSQL_DATABASE
              value: pizzadatabase
            - name: MYSQL_USER
              value: myuser
            - name: MYSQL_PASSWORD
              value: secret
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: mysql-customer-persistent-storage
              mountPath: /var/lib/mysql-customer
      volumes:
        - name: mysql-customer-persistent-storage
          persistentVolumeClaim:
            claimName: mysql-customer-pv-claim
      restartPolicy: Always

---

apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-customer-pv-volume
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-customer-pv-claim
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi



