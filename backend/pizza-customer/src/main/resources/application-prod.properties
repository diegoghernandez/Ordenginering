spring.datasource.url=jdbc:mysql://${MYSQL_DOMAIN}/pizzadatabase?createDatabaseIfNotExist=true
spring.datasource.username=${MYSQL_USERNAME}
spring.datasource.password=${MYSQL_PASSWORD}

cookie.secure=true
cookie.same-site=Strict